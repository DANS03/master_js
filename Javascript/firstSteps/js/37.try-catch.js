'use strict'

console.log('welcome to try catch')

//IF the code hase some error or something fails in order to avoid a crash we can use a try catch

try {
    var year = 2022;
    console.log(yea);
} catch (error) {
    console.log(error);
}