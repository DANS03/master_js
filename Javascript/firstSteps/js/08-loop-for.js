'use strict'

var intro = "Welcome to 08 loop for";

//Is control structure that repeats an action a given number of times

var limit = 100;

for (var i = 0; i < 100; i++) {
    console.log(i + 1);

    //debbuger --this instruction can stop the program each times the code finds it
}